<!-- #ZEROPS_EXTRACT_START:intro# -->
The managed database only — run tinbase on your laptop against it over the Zerops
VPN (`zcli vpn up`). Point DATABASE_URL at db's superuser connection and `npm start`.
Only the durable database lives in the cloud.
<!-- #ZEROPS_EXTRACT_END:intro# -->
